# rag-cost-control-layer
# https://github.com/Emmimal/rag-cost-control-layer
#
# Runtime dependencies: NONE
# All components (SemanticCache, QueryRouter, TokenBudget, CostLedger,
# CircuitBreaker, BudgetEnforcer) run on the Python standard library only.
# Python 3.10+ required.
#
# ── OpenAI integration (required for OpenAI API calls) ───────────────────────
openai>=1.30.0
tiktoken>=0.7.0          # exact token counts instead of char/4 heuristic
httpx>=0.27.0            # async HTTP client (OpenAI dependency)
#
# ── Optional: persistent storage ────────────────────────────────────────────
# redis>=5.0.0            # Cache + CostLedger persistence
#
# ── Optional: alternative LLM providers ─────────────────────────────────────
# anthropic>=0.28.0      # Claude models via Anthropic API
# boto3>=1.34.0          # Amazon Bedrock
#
# ── Development ──────────────────────────────────────────────────────────────
# pytest>=8.0.0
# pytest-cov>=5.0.0